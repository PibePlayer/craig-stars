/// <summary>
/// Each hull has a type to it so the AI knows what to do with it.
/// </summary>
public enum TechHullType
{
    Scout,
    Colonizer,
    Bomber,
    Fighter,
    CapitalShip,
    Freighter,
    ArmedFreighter,
    FuelTransport,
    Miner,
    MineLayer,
    Starbase,
}
