using Godot;

namespace CraigStars.Server
{
    /// <summary>
    /// Common interface for local and network servers. (not currently being utilized)
    /// TODO: Move the game manager logic into this interface so we can ask a server what games are available
    /// to join
    /// </summary>
    public interface IServer
    {

    }
}